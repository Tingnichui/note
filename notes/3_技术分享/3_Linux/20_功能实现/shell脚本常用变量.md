|                                  | 用途             | 用例                                      |
| -------------------------------- | ---------------- | ----------------------------------------- |
| $(dirname "$(readlink -f "$0")") | 脚本所在绝对路径 | LOG_PATH=$(dirname "$(readlink -f "$0")") |
|                                  |                  |                                           |
|                                  |                  |                                           |

